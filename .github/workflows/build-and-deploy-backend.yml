name: Build and Deploy Backend

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/backend/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # TODO: Add azure/login@v2 with OIDC
      # TODO: Login to ACR
      # TODO: Build and push Docker image
      # TODO: Update Web App container configuration
